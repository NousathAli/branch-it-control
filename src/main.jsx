import React from 'react';
import { createRoot } from 'react-dom/client';
import ITAssetDashboardPreview from './App.jsx';

createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ITAssetDashboardPreview />
  </React.StrictMode>
);
